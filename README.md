# Track-ip


1. Install Dependencies di Termux:
```
pkg update && pkg upgrade -y
pkg install python python-pip git -y
pip install requests colorama
```

2. Run script
```
# Clone repo
git clone https://github.com/AN0MALIXGR/Track-ip.git

# Masuk ke folder
cd Track-ip

# Lihat isi
ls -la
```
